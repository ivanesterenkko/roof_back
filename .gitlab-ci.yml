
stages:
  - build
  - test
  - deploy

build-job:
  stage: build
  script:
    - docker build -t roof_image .

test-job:
  stage: test
  script:
    - docker run --rm -d roof_image

deploy-job:
  stage: deploy
  script:
    - docker compose up -d
